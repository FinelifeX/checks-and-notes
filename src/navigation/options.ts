import { StackNavigationOptions } from '@react-navigation/stack';

export const rootNavigatorScreenOptions: StackNavigationOptions = {
  headerShown: false,
};

export const splashScreenOptions: StackNavigationOptions = {
  headerShown: false,
};
