import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  layout: {
    paddingVertical: 0,
    paddingHorizontal: 0,
  },
  navBarControls: {
    flexDirection: 'row',
  },
});

export default styles;
