import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  topBarContainer: {
    flex: 0,
  },
  layout: {
    paddingVertical: 0,
    paddingHorizontal: 0,
  },
  navBarControls: {
    flexDirection: 'row',
  },
});

export default styles;
